<h2>タイトル</h2>

<!-- 以下の1行を追加 -->
<%= flash[:notice] %>

<p><%= @list.title %></p>
<h2>本文</h2>
<p><%= @list.body %></p>

<!-- ここからコードを書きましょう -->
<h2>画像</h2>
<%= attachment_image_tag @list, :image, :fill, 300, 300, format: 'jpeg' %>
<!-- ここまでのあいだにコードを書きましょう -->

<%= link_to "編集", edit_todolist_path(@list.id) %>